export const metadata = {
  title: "Login | ChannelIncome",
  description: "Sign in to your ChannelIncome account.",
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
