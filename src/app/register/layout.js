export const metadata = {
  title: "Register | ChannelIncome",
  description: "Create your ChannelIncome account.",
  robots: { index: false, follow: false },
};

export default function RegisterLayout({ children }) {
  return children;
}
